// será criado a tabela onde listará todos os objetos registrados no db.json e adicionado os botões de update e delete, utilize bootstrap para criação da tabela e utilize a função Map() para criação de cada elemento do array de elementos

import "../../css/reset.css"

const Listar = () => {
    return(
        <div> Olá </div>
    )
}

export default Listar